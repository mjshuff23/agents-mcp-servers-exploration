# These defaults are enough to explore the playground without any external credentials.
AGENTS_STUDIO_PORT=4321
AGENTS_DATA_DIR=.studio-data
AGENTS_DEFAULT_MODE=deterministic
# Optional: enables live sampling when you want the client to call a real model.
OPENAI_API_KEY=
